{
    email {$CADDY_EMAIL}
}

{$CADDY_DOMAIN} {
    encode gzip zstd

    # API + WebSockets
    @api path /v1/* /healthz /ws /ws/*
    handle @api {
        reverse_proxy api:8000
    }

    # Everything else hits Next.js
    handle {
        reverse_proxy web:3000
    }
}
